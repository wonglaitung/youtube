# 金融 RAG 系列2：数据接入层

## 引言

前几天我们聊到金融 RAG 项目在金融行业落地是比较困难的事情。很多项目最初看起来挺智能的，能够达到演示项目，但实际上在最近落地的具体应用中面临足够的困难。

今天我们来拆解一下关于**数据接入层**有什么探索。

## 普通知识库 vs 金融 RAG

### 核心区别
- **普通知识库**：追求的是“可用”
- **金融 RAG**：追求的是“可被追责”

这决定了两者的架构完全不同。关键不在于模型会不会说话，而在于它说的是不是真的。如果模型说的看起来像真的，但引用的是错误数据，那就是风险。

### 普通 RAG 的问题
普通 RAG 的流程：把文档、数据切成小块，然后通过检索引擎搜到之后，交给模型生成答案。

**核心问题**：不是模型不会说话，而是它用错了数据。

**典型错误**：
- 用了过去的制度
- 把培训材料当成政策制度
- 引用了失效的文档

## 可信数据的核心要素

对于金融 RAG，我们需要的是**可信的数据对象**，确保后续生成的答案都能知道它来源于哪里。

### 可信数据的三个特征
1. **来源可信**：数据从哪里来
2. **权限可控**：对应的用户有权限访问
3. **结构可追溯**：完整的结构可以追溯、可以复核

### 具体要求
- 数据适用于什么样的理解
- 属于哪些结论
- 属于的规则是由哪些政策的制定者
- 是否有效
- 现在是否生效

## 从文档回答到证据系统

### 技术演进
从 2022 年的技术实现，从文档回答已经到了**证据选择的系统**。

传统 RAG 系统不能够再停留在“向量检索 + 大模型生成”的模式，需要新的解决方案。

### 五种关键技术

#### 1. 混合证据检索
不能依赖单一向量检索，需要支持多种数据源：
- **文档**：PDF、Word 等
- **图像**：图片、扫描件
- **视频、音频**：多媒体数据
- **数据库信息**：结构化数据

**数据库信息**才是相对更容易整合、更容易贴近实时的数据。

#### 2. 上下文增强
结合传统的 BM25% 检索方法，加上 Reranker（重排序器），可以将召回率从 5% 提升到 19%。

**问题**：
- 商业文档如何放入系统？
- 除了检索方法，还有哪些构建上下文的方法？

#### 3. 深度文档理解
需要对文档中的图形、表格、公式等对象进行深度解析。

**要求**：
- 识别文档的阅读顺序（从上往下、从左往右）
- 提取关键信息（表格数据、图表数据）
- 保持文档结构的完整性

#### 4. 全生命周期管理
从数据进入系统开始，就需要：
- 风险分类
- 来源登记
- 权限控制
- 版本管理
- 审计追踪

#### 5. 金融监管融合
考虑 AI 的第三方风险、内控流程等监管要求。

## 普通系统 vs 金融系统

| 维度 | 普通系统 | 金融系统 |
|------|----------|----------|
| 错误后果 | 不满意就算了 | 可能面临客户投诉、监管处罚 |
| 数据要求 | 可用即可 | 可信、可追溯、可审计 |
| 关键点 | 模型会不会说话 | 说的是不是真的 |

**金融 RAG 的关键点**：比风险、比处罚。我们希望把系统做得更加完善、更加可靠。

## 金融 RAG 数据接入层的九段式架构

### 核心目标
解决几个关键问题：
1. **来源可信**：数据从哪里来
2. **风险分类**：数据的风险等级
3. **动态路由**：根据数据类型选择处理路径
4. **正确解析**：多模态数据的正确解析
5. **质量评估**：数据质量的评估和验证

### 九段式架构详解

#### 1. 来源登记
数据来源可能包括：
- 业务系统
- 人工上传
- 第三方数据
- 外部数据
- MCP 数据

需要记录数据的来源、结构、类型等信息。

#### 2. 风险分类
不仅仅是获取数据，还要进行风险评估：
- 低风险：公司公开报告、规章制度
- 高风险：客户交易数据、员工信息、客户个人信息

#### 3. 动态信息路由
把文档分成两类：
- **简单文档**：可以直接解析得到最终结果
- **复杂文档**：需要识别阅读顺序、包含多种对象（表格、图片、音频等）

#### 4. 多模态解析
金融文档中包含多种数据类型：
- 文本数据
- 表格数据
- 图像数据
- 音频数据

需要针对不同类型的数据采用不同的解析方法：
- **文本**：PyMuPDF 等工具
- **结构化数据**：数据库直连
- **复杂版式**：LlamaParse 等工具
- **中英文混合**：需要特别注意

#### 5. 正确解析与验证
解析后需要验证数据的正确性：
- 人工审核
- AI 辅助分析
- 数据类型识别

#### 6. 质量评估与入库存储
最终入库存储前需要：
- 质量评估
- 信息过滤
- 元数据标记

#### 7. 索引与服务
将数据索引到对应的数据库：
- 向量数据库
- 图数据库
- 关系型数据库

#### 8. 审计与追溯
保持完整的审计记录：
- 数据来源
- 处理过程
- 版本信息
- 使用记录

#### 9. 复盘与优化
通过审计回放，复盘整个过程：
- 发生了什么
- 过程是什么
- 找回了什么
- 回答用过的是什么

## 多模态数据处理

### 文档类型识别
一份文档可能包含：
- 文本
- 表格
- 图片
- 音频
- 视频

### 解析方法选择
- **纯文本文档**：PyMuPDF 直接提取
- **结构化数据**：数据库连接
- **复杂版式**：LlamaParse 等商业工具
- **中英文混合**：需要特殊处理

### 关键点
1. 识别文档的阅读顺序
2. 提取表格中的数据
3. 识别图片中的文字（OCR）
4. 保持文档结构的完整性

## 实践建议

### 数据准备原则
1. **内部数据优先**：把自己领域的主数据整理得比任何人都权威
2. **外部数据补充**：在内部数据基础上，再整合外部数据
3. **质量优先**：确保数据的准确性和时效性

### 技术选型建议
- **纯文本文档**：PyMuPDF
- **结构化数据**：数据库连接
- **复杂版式**：LlamaParse 等商业工具
- **中英文混合**：需要特殊处理

### 风险控制
1. **权限控制**：确保数据访问权限
2. **版本管理**：确保使用最新有效版本
3. **审计追踪**：保持完整的处理记录

## 结论

金融 RAG 数据接入层的核心是**数据可信性**。企业内部可能拥有所有金融行业的数据，但来自自己领域的数据应该整理得比任何人更权威。

在此基础上，再整合外部数据和其他 MCP 数据，才能得到更准确、更可信的结论。

**这是数据接入层最重要的结论。**