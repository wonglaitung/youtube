# 金融 RAG 系列3：最容易翻车的地方——不是模型，而是文档分片做错了

## 引言

在金融 RAG（检索增强生成）系统中，最常见的失败原因并非模型能力不足，而是文档分片策略不当。模型越强大，越容易将错误信息表述得如同真实，这使得分片质量成为系统可靠性的关键。

## 问题现象

### 案例一：信息遗漏
客户询问：“意外住院能报销多少？”
AI 回答：“这个要赔付十万。”
听起来正确，但遗漏了关键条件：“必须在事故发生后三十天内，提交完整的病例和诊断证明。”

### 案例二：条件缺失
销售问：“这款产品的年化收益率是多少？”
AI 回答：“年化收益率 13%。”
但未说明限制条件：“仅限优质信用客户，可能还需要购买保险。”

### 核心问题
金融 RAG 真正的挑战不是让 AI 找到一句话，而是让 AI 找到**完整的业务条件**，包括：
- 额度
- 限制
- 版本
- 权限
- 使用对象

这些信息必须被**一起找到**，否则答案将从“有用”变为“危险”。

## 文档分片的本质

### 传统分片的误区
很多人认为文档分片就是将长文档切成小块，例如每 500 个 token 切一块，每块重叠 100 个 token，然后向量化存储。这是早期 RAG 系统的常见做法。

但**金融文档不是普通文档**：
- 关键信息往往不是孤立存在的
- 一句“赔付十万”可能跟着很多条件
- 一个“年化收益率 3%”的产品可能有很多要求
- 一个表格里的数字需要结合表头、单位、使用地区等才能理解
- 一个合规条款可能只适用于某类客户、某个地区或某个时间范围

如果仅按固定长度切分，很容易：
- 把结论和条件切开
- 把正文和表格切断
- 把条款和例外切断

### 金融 RAG 分片的核心
金融 RAG 中的文档分片不是简单的“切分文本”，而是**知识整理**。系统需要将一份复杂的文档转化为：
- 可被检索
- 可被理解
- 可被引用
- 可被追溯的知识单元

这个知识单元不仅包含正文，还包括：
- 标题层级
- 表格位置
- 文件来源
- 版本号
- 生效日期
- 上下文关系

## 分片策略的五个关键步骤

### 第一步：分析文档结构
识别文档的结构信息：
- 章节
- 表格
- 标题层级
- 所属产品、版本、页面等元数据

### 第二步：根据业务问题决定切分方式
- **产品简介**：可以切得稍大一些
- **合规条款**：必须保存完整的条件
- **表格**：需要把表头、单位、行列关系一起保存
- **理赔规则**：不能把理赔额度和身体条件分开

### 第三步：为每个块添加上下文
每个知识块必须包含：
- 来源文档
- 所属章节
- 前文内容
- 是否为最新版本

没有上下文，AI 看到的只是孤立的文字；有了上下文，才能看到有身份、有位置、有边界的知识。

### 第四步：组合检索策略
不能只依赖向量相似度，需要结合：
- **关键词检索**：产品编号、条款名称、客户类型
- **语义检索**：用户口语表达的复杂业务问题
- **结构化过滤**：只查最新版本、特定地区、特定部门有权限的内容

更可靠的系统往往采用：关键词 + 向量检索 + 元素过滤 + 重排序 + 关系提取的组合策略。

### 第五步：用真实问题反向优化分片
评估标准：
- 真实问题能否命中关键信息
- 是否漏掉了限制条件
- 引用是否准确
- 有无使用过期版本
- 业务人员是否真的敢用

如果评测发现：
- 常见问题经常漏掉审批材料 → 分片策略没有保住条件
- 贷款产品经常漏掉适用客户 → 分块的上下文不够
- 引用经常对不上 → 页码、章节、锚点都没有处理好

## 普通 RAG 与金融 RAG 的区别

| 维度 | 普通 RAG | 金融 RAG |
|------|----------|----------|
| 目标 | 把文档切碎，方便 AI 查找 | 把文档的内层证据组织好，方便 AI 负责 |
| 分片方式 | 固定长度切分 | 结构感知、语义感知、业务感知、评测驱动 |
| 上下文 | 基本不考虑 | 必须包含来源、版本、章节等完整上下文 |
| 评估标准 | 能找到相关信息 | 信息完整、条件齐全、引用准确、版本正确 |

## 结论

文档分片不是一个小技术点，它决定了金融 RAG 系统到底是在做“看起来聪明的检索”，还是在做“可以被信任的知识系统”。

一个优秀的 RAG 系统，最后看起来好像是 AI 在回答问题，但背后真正决定质量的，往往是这些看不见的工作：
- 文档有没有解析对
- 表格有没有还原对
- 章节有没有保住
- 版本有没有治理
- 权限有没有过滤
- 分块有没有带上上下文
- 检索有没有到位到完整的证据
- 引用有没有落到准确的位置上

**模型越强，越容易把缺失的信息讲得像真的。** 因此，在金融场景中，真正可靠的 RAG 不是让 AI 自由发挥，而是让 AI 在一套可追溯、可审计的结构化知识体系中工作。