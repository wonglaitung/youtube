# 金融 RAG 系列5：拆解金融级 RAG 数据召回层——别让智能变成合规风险

## 引言

金融 RAG（检索增强生成）与普通 RAG 有本质区别。普通 RAG 只需要找到与问题相似的文本，而金融 RAG 必须找到能够支撑后续判断的**候选证据**。

### 核心区别
- **普通 RAG**：找到与问题相似的文本
- **金融 RAG**：找到能够支撑业务判断的完整证据链

### 示例：提前赎回问题
客户问：“这个产品提前赎回会不会亏钱？”

**普通 RAG 回答**：“本产品支持提前赎回。”
**金融 RAG 必须找到**：
- 赎回费率
- 开放期限制
- 净值波动风险
- 是否保本
- 当前文档是否为最新有效版本

**如果只找到“支持提前赎回”这一段，后续客户问“可以赎回吗？”就会产生合规风险。**

## 金融召回层的核心职责

金融 RAG 的第四层**数据召回层**，其职责不是生成最终答案，也不是判断论据是否成立，而是：

**根据第三层的查询规划，在正确的数据源、正确的权限范围和正确的版本条件下，召回一批可靠的候选证据。**

### 金融召回必须同时关注三件事
1. **相关性**：够不够高
2. **关键证据**：有没有漏
3. **版本权限**：是不是正确

## 为什么普通向量检索不够用

### 问题一：语义相似不等于业务正确
找到“提前赎回”这一段，但遗漏了费率、限制条件、版本信息，**相关但不完整，等于直接有风险**。

### 问题二：Top-K 不等于证据完整
很多 RAG 为了提高召回率，把 Top-K 调大（从 50 到 150 甚至更大）。但如果召回路径错了，Top-K 调得再大也没用。

**如果系统召回回来的全是产品介绍，没有风险提示、费用表、适当性规则，候选证据依然是缺的。**

### 问题三：权限和版本不能后置过滤
很多系统习惯“先搜索，再过滤”，这在普通场景可以接受，但在金融场景**万万不可**。

- 涉及权限、版本的信息，不能让模型下一步再用
- 更不能把已经失效的产品说明书、过时的制度和当前的规则混在一起

### 问题四：数据不在单一数据源
金融数据分散在多个系统：
- 产品的风险等级可能在产品数据库
- 客户的风险等级可能在客户数据库
- 交易记录、持仓数据在不同的业务系统

**如果只查一个向量库，很多关键证据就找不到。**

## 四类核心召回路径

### 1. 文本召回
适用于简单的产品介绍问题，可能走关键词检索或向量检索的混合搜索。

- **关键词检索**：处理产品代码、活动编号等精确匹配
- **向量检索**：处理语义差异（如“提前取钱” vs “提前赎回”）
- **元素过滤**：权限、版本、区域等约束条件
- **融合机制**：把几种检索的优点组合，生成候选数据

### 2. 结构化数据召回
从交易系统、客户主数据库等通过 API 或视图读取。

**关键点**：不能让模型直接连接生产库，更不能让模型自由生成 SQL。

- 使用 Text-to-SQL 技术生成查询
- 但必须有**权限控制**：能查什么表、用什么条件、返回什么数据
- 不能看到客户的身份信息、不能访问交易明细
- 结果需要脱敏

### 3. 复杂文档召回
金融文档中有大量 PDF、扫描件、表格数据。简单的 OCR 或切块会丢失结构。

**示例**：表格中单位是“百万元”，脚注写着“不含非金融性收益”。如果这些信息丢失，模型后面就会答错。

**解决方案**：
- 识别 PDF 的阅读习惯（从上到下、从左到右）
- OCR 结合表格结构解析、图表解析
- 基于已建好的索引（页面级、区域级、表格级）进行稳定召回

### 4. 图谱和关系召回
有些问题需要沿着关系链找证据，例如：
- 公司和子公司之间的股权关系
- 产品和条款之间的关系
- 公司之间的担保关系
- 制度与制度之间的上下游关系

**向量擅长找相似，不擅长沿关系进行查询。** 因此需要：
- 实体识别（可能需要用专门的模型）
- 将客户、产品、公司等映射到标准 ID
- 在图数据库或关系服务中查询一跳或多跳关系

## 受控召回的四个关键控制点

### 1. 权限前置
用户不能访问的材料，**绝对不能先召回回来**。如果不能看的相关信息或敏感信息，绝对不能流到下一步。

### 2. 版本前置
查询的必须是**有效版本**，而不是引用错误的版本。

### 3. 数据源前置
不同问题只能访问必要的数据源：
- 普通产品介绍不需要访问客户交易系统
- 条款核对不能访问客户画像系统

**最小必要原则。**

### 4. 过程可溯源
必须记录：
- 哪个服务访问了哪个查询服务
- 使用了哪些过滤条件
- 证据来源于哪些数据源
- 版本、生效日期分别是什么

这些记录用于后续的审计和复盘。

## 召回结果标准化

不同来源的召回结果格式各异：
- 文本片段
- SQL 查询返回的一行记录
- 图数据库的节点和关系
- 一页页码化的扫描文档

**必须把不同来源的结果统一成一个标准化的候选证据对象**，包含：
- 证据 ID
- 数据源类型
- 权限标签
- 版本信息
- 时间信息
- 实体 ID
- 文字摘要
- 页码、表格对应的位置

**核心价值**：输出的不是答案，而是一批来源清楚、权限核实、版本正确、可以被后续精排或审计的候选证据。

## 评估指标

金融 RAG 的召回层必须单独评估，不能只看最终答案好不好。至少要看：

1. **关键证据的召回率**：关键证据有没有召回
2. **证据的覆盖率**：证据覆盖是否全面
3. **权限召回错误率**：权限召回有没有错误
4. **版本错误率**：有没有把失效版本、历史版本和当前版本混在一起
5. **结构化命中率**：结构化数据查询时，关键信息有没有确定得到
6. **无答案率**：曾经下面的误报率

**最终目的**：让召回层的数据证据不是多找一点，而是找对、找全、找合法的、找有效的。

## 结论

### 传统 RAG vs 金融 RAG 召回层

| 维度 | 传统 RAG | 金融 RAG |
|------|----------|----------|
| 流程 | 用户问一句，系统查一次 | 接收查询规划，拆解为明确的召回任务 |
| 数据源 | 单一向量库 | 多数据源（文本、结构化、图谱） |
| 控制 | 基本没有 | 权限前置、版本前置、数据源前置 |
| 输出 | 相似的文本片段 | 标准化的候选证据对象 |
| 目标 | 找到最像问题的文本 | 在正确版本、正确数据源里找出正确证据 |

**金融 RAG 的召回层不是向量检索的升级版，而是受控证据的获取系统。** 它不直接给答案，而是负责把可以引用、可以审计的证据找出来，供后续的精排、生成和审计使用。