# 金融 RAG 系列4：硬核拆解查询规划——干掉合规灾难与数据穿越

## 引言

在金融 RAG 系统中，查询规划层是确保系统可靠性的关键。很多人做金融 RAG 的第一反应是：把文档切碎、做成向量、丢进向量数据库，最终让模型检索回答。但这种做法在金融行业是在“埋地雷”。

## 核心问题：只答对一半的危险

### 真实案例
客户问：“这个保险产品，意外住院能不能报销？”
系统回答：“可以报销，最高赔付十万。”

听起来完美，但系统遗漏了：
1. 必须在事故发生后 48 小时内报案
2. 这条条款不适用 65 岁以上的客户
3. 这个版本的条款已经作废，被新版本替代

**这不叫回答，这叫合规灾难。**

### 金融 RAG 的核心痛点
从来不是有没有收到一句相关文本，而是：
- 系统到底有没有理解用户真正问的是什么
- 有没有把限制条件、时间、版本、权限、证据一次性完整查出来

## 查询规划层的定位

金融 RAG 的第三层**结构化查询规划层**，不是简单的“查询改写”，而是**受控的查询编排层**。

### 核心任务
在检索发生之前，把用户的自然语言翻译成一份：
- 有边界、受约束
- 可审计
- 结构化的执行计划

### 设计原则
**不是让模型想做什么就做什么，而是用工程手段让它少乱想，按规矩办事。**

## 为什么纯向量检索在金融场景是灾难

金融问题不是一个单纯的语义相似度问题，而是**多约束的检索问题**，同时包含：
- 实体（公司、产品）
- 时间版本
- 权限
- 业务规则
- 证据来源

如果把这些约束都放进一个 query embedding 里，在向量空间进行排序，会发生严重的**信息丢失**。

### 示例
- 2006 年的统一笔征数 vs 2001 年的统一笔征数
- 在向量空间里可能只有一个五和六的差别
- 但在金融计算里，算错一个数，投资报告、估值模型就彻底废掉

## 查询理解 vs 查询规划

| 概念 | 定义 | 示例 |
|------|------|------|
| **查询理解** | 语义解析，提取查询意图 | 谁问了什么指标，在什么时候 |
| **查询规划** | 行动路线的构建 | 第一步去哪查，第二步合并什么，第三步怎么校验 |

## 金融 RAG 查询规划的六大工程支柱

### 1. 意图分类
用户的问题在系统里绝对不能一视同仁：
- **对比查询**：“工行和建行的核心资本充足率有什么区别？” → 需要多个实体并行检索
- **结构化时序查询**：“过去三年研发费用变化” → 需要走 SQL 查询
- **非结构化条款查询**：“合规部门对反洗钱的内部审计要求是什么？” → 需要走向量检索

**如果系统不判断意图，直接拿所有问题去向量库碰运气，复杂条件一定会漏掉。**

### 2. 实体和指标的确定性解析
金融行业怕的是“差不多”：
- 用户说“零王”，系统必须知道这是“宁德时代”，并关联股票代码
- 用户说“坏账”，系统要知道对应的是“信用减值损失”还是“不良贷款率”

**正确做法**：把实体解析、指标解析做成确定性的 API，让规划器在指定规则下调用工具匹配标准元数据。

### 3. 时间轴的解析（最致命）
用户问：“最近一期的财报是多少？”
如果今天是 2016 年 5 月 19 日，大模型直接拿“最近一期”去搜，很容易收到预测数据。

**必须通过确定性的时间解析器**：
- 数据的观测点
- 数据的生效时间
- 系统自动生成硬性过滤：发布时间必须在 2016 年 5 月 19 日之前

**防止前视偏差、数据穿越，这是投资决策和合规审查的绝对红线。**

### 4. 数据源的动态路由
**“万物向量化”是工业界最大的谎言。**

金融机构大量的核心数据并不适合传统向量数据库：
- **交易数据、客户数据、市场数据、财务指标** → 适合大数据平台、专门的业务系统
- **股权关系、关联方、担保链** → 适合图数据库
- **制度条款、研报、审批材料** → 适合文档检索和向量召回

一个完美的 Query Plan 应该有一张路由蓝图：
- 股权穿透走数据库
- 财务指标走 SQL 数据库
- 条款限制、合规风险才走向量数据库

### 5. 临时权限的权限前置
金融最大的安全漏洞就是“先搜出来，然后再过滤”。

**绝对不能让大模型来做权限的裁判**，而是让底层系统从源头限制它能检索什么。

### 6. 归一化元数据的预设
金融监管讲究的是可审计。任何返回的检索片段都必须带上物理属性：
- 文档 ID
- 版本号
- 生效时间
- 页码、章节、路径

没有这些作为支撑，大模型生成的答案没有任何参考价值。

## 架构设计：分层规划与专门 Agent 协作

不要迷信复杂的 Agent，行业里有两个极端：
1. 完全不用 Agent，靠一次向量检索撞大运
2. 一上来就堆满 Agent，多智能体、多状态机，让几个模型在后台反复讨论

**两种方向都不对。**

### 成熟架构：分层规划与专门 Agent 协作

#### 第一层：入口分类器
先判断用户问的是：
- 条款解释
- 指标查询
- 对比分析
- 风险验证
- 跨文档的综合问题

#### 第二层：专门理解 Agent 或确定性工具
- **实体解析 Agent**：把“宁德时代”解析为“宁德时代”，并对上股票代码
- **指标解析 Agent**：识别“研发费用”是资本化支出还是费用化支出
- **时间解析 Agent**：处理“最近一期”、“最近三年”、“截止某天”等表达
- **权限判断 Agent**：判断能看到哪些文档和客户的范围
- **数据源路由 Agent**：决定走 SQL、图数据库、向量查询还是混合检索

#### 第三层：计划组装器
不让多个 Agent 自由聊天，而是把各个专门模块的结果组装成一份受控的 Query Plan。

这个计划必须写清楚：
- 查什么实体
- 查什么指标
- 用哪个时间窗口
- 用哪个版本
- 访问哪些数据源
- 需要什么证据

#### 第四层：执行层
执行层按照计划去查 SQL、查向量库、查文档，它不负责重新理解用户问题，只负责执行并获取结果。

#### 第五层：验证器
检查：
- 实体是否一致
- 时间是否一致
- 版本是否有效
- 权限是否越界
- 证据是否足够支撑答案

**金融 RAG 的查询不是简单的快慢路径，它更像一个受控的多专家系统。**

## 上下文辅助召回：与查询规划的配合

### 技术背景
传统的 RAG 在把文档切碎之后，由于片段丢失了前因后果，召回率比较低。

在切片时，给每个片段补一段上下文说明，让它不再是一个孤立的片段。

**示例**：这段话来自于苹果公司 2025 年财报的研发费用章节。

### 与查询规划的关系
如果没有第三层的查询规划配合，上下文召回就是摆设。

**因为你的文档片段上带上了这些身份的标签时，你的查询规划在生成 Query Plan 就必须主动提取出这些元素作为过滤条件。**

只有当两者在检索时完美对齐，上下文查询的威力才能真正发挥出来。

## 落地指南：五条规定

### 1. 输出强制用 JSON 约束
千万不要让模型自由输出。利用大模型的结构化输出功能，必须强制它输出为严格的 JSON 格式。格式不对，直接在网关层就可以拒绝。

### 2. 单独评估第三层
别只看到最终回答的好不好，还要把意图分类、实体解析、时间转换拿出来单独做评测。第三层准确率达不到 99%，后边召回再好也是在错误的基础上做。

### 3. 规划和生成的职责分离
写规划的模型不要去做生成，生成的模型不要去总结。各司其职。

### 4. 保留不可篡改的审计日志
把全流程的用户从原始提问开始，生成计划，开始去调用实际的工具，命中的文档版本，对应的数据都需要做好日志记录。

### 5. 预设优雅的降级策略
如果是 SQL 数据库查询超时，或者规划层遇到了无法识别的指令，系统不能挂。必须在某些节点自动降级为泛化的语义搜索，并提示用户，做到“知错就改”。

## 结论

普通 RAG 依然依赖语义相似度，但金融级的 RAG 追求的是**受约束的业务证据链**：
- 权限要先设置
- 版本要能追溯
- 证据要能复核
- 答案要能被审计

查询规划层就是在这两条红线之间，最关键也是最牢固的桥梁。

它的价值就是把大模型的生成能力，框进可约束的工程实践里面。系统不是凭感觉回答，而是按版本、按权限、按业务规则、证据来进行回答。