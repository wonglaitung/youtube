# 金融 RAG 系列6：金融 RAG 最容易翻车的一层——数据精排不是 Rerank，而是证据风控

## 引言

很多金融 RAG 项目不是在生成层出问题，而是在生成之前。我们之前讲到：
- **第一层数据接入层**：如果没有保留数据的血缘关系，模型拿到的可能数字跟单位对不上，表头和页码对不上
- **第二层数据分片**：如果没有自包含语义的注入，每个分片可能只写了“该比例不低于 5%”，第五层根本不知道该比例是什么
- **第三层查询规划**：如果没有保护问题变异层，系统就分不清用户要的是监管原文、公司公告还是研报观点
- **第四层数据召回**：如果只把向量、编辑而图谱、结构堆在一起，第五层就是一锅乱炖

**所以第五层的金融精排不是简单的 Rerank。**

## 金融精排的核心任务

它真正要做的是在候选证据进入生成层之前：
- 哪些是主证据
- 哪些是限制条件
- 哪些是例外条款
- 哪些是旧版本
- 哪些是必须标记的冲突

**一句话：召回层决定能不能找到证据，第五层决定会不会用错数据。**

## 为什么金融精排如此重要

### 真实案例
用户问：“这个产品能不能提前赎回？”
系统搜完后模型回答：“可以。”
听起来没问题，但实际上赎回需要持有 180 天，而且在封闭期内不能赎回。

**这就是金融场景下的 RAG 难做的原因。**

### 核心问题
金融 RAG 怕的不是收不到，而是收到了一堆看起来都相关的材料，而最后模型却调错了对应的证据。

如果只是把最像用户问题的片段排在前面，系统会回答得很流畅，但也错得很危险。

## 金融精排的本质：证据包

金融 RAG 的第五层数据精排，不能只是把相似最高的内容排在最前面，而应该是一个**可以审计的证据包**。

### 证据包必须包含
1. **主证据**：直接回答问题的核心证据
2. **定义证据**：条款的定义、解释
3. **限制条件**：适用范围、例外情况
4. **例外条款**：特殊情况的处理
5. **生效时间**：版本的时间关系
6. **冲突材料**：相互矛盾的证据
7. **引用关系**：证据之间的引用

**金融精排不是为了选出相关的一段话，更是为了组织不完整的证据，这才是它的核心。**

## 五步构建金融精排系统

### 第一步：用规则和元数据建立前序

**不要直接上大模型！** 先用规则和元数据建立前序。

#### 金融材料的天然区别
- **监管原文**比媒体解读更权威
- **公司公告**比研报观点更权威
- **终稿**比草案也要更权威
- **当天的有效版本**比过期版本更重要
- **原始条款**比别人总结出来的再要更可靠

#### 元数据检查清单
在金融精排之前，系统要先看这些元数据：
- 这些材料来源于哪里
- 是不是正式文件
- 已经审批
- 当天是否生效
- 有没有过期
- 适用于哪个产品、哪个客户
- 哪类客户、哪个区域
- 有没有引用的矛盾

**这些东西能不能交给大模型？还是说它们就是已经完全确定的数据？**

### 第二步：用专用模型做主路径精排

可以用 Qwen 的本地模型，也可以用客户的模型。中间层尽量不要一上来就把所有的候选都交给 GPT 或 Claude。

**原因很简单**：大模型很强，但是很慢，而且不一定比专用的模型更适合。

#### 合作大规模的候选排序流程
1. **轻量模型**：用 BGE 或其他模型把候选从 500 压到 100
2. **中间模型**：把候选 100 压缩到候选 30
3. **大模型**：把高风险的问题，或者用更强的大模型来负责这候选 10 左右的证据

**这样才能在成本、延迟和准确性之间取得平衡。**

### 第三步：证据角色识别

这是金融 RAG 和普通 RAG 最大的区别之一。

普通 RAG 只问：“这段内容相关吗？”
金融 RAG 还要问：“这段内容在答案里扮演什么角色？”

#### 证据角色类型
- **主证据**：直接向回答问题的核心证据
- **观点材料**：补充说明的观点
- **冲突证据**：相互矛盾的证据
- **定义条款**：解释概念的条款
- **限制条款**：适用范围和条件
- **例外条款**：特殊情况的处理
- **生效时间**：版本的时间关系

#### 示例
用户问：“这个客户能不能获得赔付？”
- **主条款**：说可以赔付
- **定义条款**：说明什么叫做合格的客户
- **限制条款**：说明必须在事故后 30 天内提交材料
- **例外条款**：说明目前情况是不能赔付的
- **生效时间**：说明这个版本从什么时候开始

**这些证据缺一不可，答案就有可能出错。**

**精排层不能够只按分数排序，它还要识别证据的角色。缺失任何一类关键的证据，都有可能导致严重的偏差。**

### 第四步：用规则动态调整证据包

很多 RAG 就喜欢用 Top-K，比如永远用 Top-10 或者永远取最终的 Top-100。这种固定方式在金融场景就很糟糕。

#### 动态调整策略
- **简单问题**：可能只需要 3-5 条证据（费率、版本、日期）
- **分析性问题**：可能需要 8-15 条证据（现金流量表、应收账款、存货、管理讨论、行业背景）
- **冲突判断问题**：保留 10-20 条，并且必须保留双方的证据

**第一阶段最稳的做法不是直接上强化学习，而是先做规则型的动态调整。**

根据问题的类型、风险的等级、证据的角色、分数的分布，动态决定证据数据的大小。

### 第五步：显式标记冲突

金融文档里的冲突很常见：
- 旧版本和新版本
- 草稿和终稿
- 监管文件和研报
- 官方公告和市场传闻
- 同一个指标在不同口径下看起来不一致

**精排层不能简单地把低分材料删掉。** 因为很多冲突并不矛盾，而是条件不同。

#### 示例
- 普通客户限额 10 万
- 高净值客户限额是 50 万

这不是冲突，这是适用范围不同。

#### 冲突标记类型
- 版本冲突
- 口径差异
- 适用观点冲突
- 一般规则和例外规则的关系
- 无法消解，必须转人工
- 后续需要披露的信息

**有了这样的标记之后，下一步在调用大模型时，大模型才能正确处理。**

## 大模型的角色定位

### 大模型的价值
大模型当然有价值，因为它擅长处理：
- 长文本
- 监管的复杂监管文件
- 多版本的制度
- 投研材料的冲突
- 例外条款的判断

### 合理的分工
- **Qwen3 Reranker 或客户 Reranker**：作为主路径的预精排
- **Qwen3.6 或 DeepSeek**：负责证据角色识别
- **DeepSeek**：负责冲突的初筛
- **大模型**：处理高风险、长文本、负责冲突和底线复核

**这才是当下金融行业 RAG 能长期运行的组合。**

## 强化学习版的 Dynamic RAG

### 适用场景
强化学习版的 RAG 解决的是更复杂的问题：
- 多跳的投研分析
- 跨文档的制度对比
- 监管要求和内部制度之间的差异
- 潜在的风险

**这些问题不是简单的排序问题，而是证据集合问题。**

### 前提条件
强化版的 RAG 必须要有前提：
- 要有线上日志
- 要有人工整合样本
- 要有金融场景下面的专家知识

**这个知识不能只讲看起来对的，还要讲引用是否支撑答案、例外的条款是否保留、版本是否正确等等。**

## 落地路线图

### 第一步：规则动态调整
用金融专家知道的这些规则先固化下来：
- 什么问题需要多少证据
- 什么场景需要保留条款
- 什么情况需要触发底线复核

**这些规则先跑起来，系统才能够有稳定的基线。**

### 第二步：监督学习版证据选择
当你线上已经有了足够的日志，人工已经标注出哪些证据是好的、哪些是差的，这时候就可以训练一个动态证据选择器。

它学习的不是哪一段内容最相关，而是哪一组数据组合起来最适合回答这个问题。

### 第三步：升级到强化学习版 Dynamic RAG
在第一步和第二步的基础上，引入强化版 RAG：
- 动态证据的选择
- 多跳分析
- 跨文档的信息对比
- 监管认识
- 冲突回答的正常运动

**用强化版的 RAG 替代部分的规则型动态调整决策。**

## 结论

金融 RAG 的精排层不是为了让模型更会说话，而是为了让模型只能在正确的证据边界里说话。

**如果这一层就不好，后面的生成模型越强，风险反而越大。** 因为它会把错误的证据说得更自然、更自信，也更难被发现。

**金融 RAG 真正的第五层不是简单的 Rerank，它是证据进入答案前最后的一道风控闸门。**